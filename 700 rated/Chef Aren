PROBLEM LINK:-https://www.codechef.com/problems/CHEFEREN?tab=statement



                                ///*   SOLUTION   *///

#pragma GCC optimize("O2,no-stack-protector,unroll-loops,fast-math")
#include <iostream>
#include <bits/stdc++.h>
#define ll long long
#define PB push_back
#define F first
#define S second
#define uset unordered_set
#define umap unordered_map
#define MP make_pair;
#define vt vector
#define all(n) begin(n), end(n)
#define sz(n) (int)n.size()
#define REP(i, n) for (int i = 0; i < n; i++)
#define pii pair<int, int>;
#define pll pair<ll, ll>;

using namespace std;

typedef vector<ll> vl;
typedef pair<ll,ll> pl;

void solve()
{
    int n,a,b;
    cin >> n >> a >> b;
    if(n&1)
    cout<<(n/2*a)+((n/2+1)*b)<<'\n';
    else
    cout<<(n/2*a)+(n/2*b)<<'\n';
}
int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0), cout.tie(0);
   	ll t;
    	cin >> t;
    	while (t--)
    	{
        	solve();
    	}
    	return 0;
}
